services:
  cf-events-api:
    build:
      context: .
      dockerfile: CF.Events.API/Dockerfile
    image: cf-events-api

  cf-events-web:
    build:
      context: .
      dockerfile: CF.Events.Web/Dockerfile
    image: cf-events-web
